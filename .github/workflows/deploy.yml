on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      DOCKER_BUILDKIT: 1

    strategy:
      matrix:
        stage:
          - minimal
          - dev

    steps:
      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@master
        with:
          install: true

      - name: Cache Docker layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache-${{ matrix.stage }}
          key: ${{ runner.os }}-buildx-${{ matrix.stage }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-${{ matrix.stage }}

      - name: Build & Push
        uses: docker/build-push-action@v2
        with:
          target: ${{ matrix.stage }}
          push: false
          # username: ${{ secrets.DOCKER_HUB_USERNAME }}
          # password: ${{ secrets.DOCKER_HUB_PASSWORD }}
          # repository: vigenere23/renv-${{ matrix.stage }}
          cache-from: type=local,src=/tmp/.buildx-cache-${{ matrix.stage }}
          cache-to: type=local,mode=max,dest=/tmp/.buildx-cache-${{ matrix.stage }}-new

      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-cache-${{ matrix.stage }}
          mv /tmp/.buildx-cache-${{ matrix.stage }}-new /tmp/.buildx-cache-${{ matrix.stage }}
